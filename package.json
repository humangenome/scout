{
  "name": "scout",
  "version": "0.1.0",
  "description": "AI voice assistant powered by Claude — open source, self-hosted, privacy-first",
  "main": "dist/server.js",
  "bin": {
    "scout": "dist/cli/scout.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.cli.json",
    "dev": "nodemon --watch src --ext ts --exec 'npm run build && node dist/server.js'",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "claude",
    "voice-assistant",
    "ai-assistant",
    "self-hosted",
    "open-source"
  ],
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^11",
    "dotenv": "^16",
    "express": "^5",
    "node-pty": "^1",
    "openai": "^4",
    "strip-ansi": "^7",
    "ws": "^8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7",
    "@types/express": "^5",
    "@types/ws": "^8",
    "nodemon": "^3",
    "typescript": "^5"
  }
}
