{
  "name": "scout",
  "version": "0.1.0",
  "description": "Voice-first Claude Code assistant — open source, self-hosted, privacy-first",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --watch src --ext ts --exec 'tsc && node dist/server.js'",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "claude",
    "voice-assistant",
    "claude-code",
    "ai-assistant",
    "self-hosted"
  ],
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16",
    "express": "^5",
    "node-pty": "^1",
    "openai": "^4",
    "strip-ansi": "^7",
    "ws": "^8"
  },
  "devDependencies": {
    "@types/express": "^5",
    "@types/ws": "^8",
    "nodemon": "^3",
    "typescript": "^5"
  }
}
